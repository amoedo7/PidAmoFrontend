# PidAmoFrontend

Repositorio histórico creado con la intención de separar el frontend de PidAmo.

## Estado

**Placeholder / separación no completada.** En este repositorio no hay actualmente una implementación frontend independiente: sólo existía un README mínimo.

La implementación sustancial de PidAmo de aquella etapa se encuentra en:

https://github.com/amoedo7/PidAmo

allí, el frontend vive principalmente en `templates/` y `static/` junto al backend Flask.

## Regla para el futuro

Si PidAmo se retoma y se decide una arquitectura frontend/backend separada, este repositorio puede reutilizarse. Hasta entonces no debe presentarse como un frontend listo para deploy.

Una futura separación debería definir explícitamente:

- framework o HTML estático;
- contrato/API con backend;
- variables de entorno;
- build/deploy;
- autenticación;
- estados offline/error;
- branding DesarrollAMO reutilizable.

---

**DesarrollAMO** · placeholder preservado para no perder historia ni URLs.
